# terrex.structures.load_net_module.*

- [x] Сделать текстовое преобразование чтобы были видны данные в логах

# proxy/proxy.py

- [x] Сделать отображение времени в txt истории пакетов
- [x] Сделать создание общего both-traffic файла с отображением какой пакет от кого пришел

# locales

- [ ] Добавить все недостающие переводы Terraria.exe

# terrex

- [x] Добавить декодер недостающих пакетов Terraria
- [x] Добавить обработчик пакетов авторизации в client.py
- [ ] Добавить logger Terrex: с именами для каждого файла откуда логи отправляются
- [ ] Добавить класс для методов движения для каждой клавиши:
    - [ ] функция отправки пакета нажатия клавиш press('W' / 'A' / 'S' / 'D' / 'Space', 'Up' / 'Down')
    - [ ] функция отправки движения (комплекса пакета клавиш с указанной задержкой, минимум по дефолту 1.5s)
        - [ ] jump() -> Space-Down, sleep(1.5s), Space-Up
        - [ ] left() -> D-Down, sleep(1.5s), D-Up
        - [ ] right() -> A-Down, sleep(1.5s), A-Up
        - [ ] down() -> S-Down, sleep(1.5s), S-Up
    - [ ] функция отправки выстрела 6.5 CPS (в среднем)
    - [ ] сделать debounce на все клавиши отправки движения, чтобы нельзя было отправлять jump() раньше пока еще не закончил выполнятся старый с warning уведомлением
- [ ] Сделать класс и подклассы для инвентаря и его подвидов и их обработчики которые будут хранится в Player->inventory
    - [ ] приватная функция отправки пакета изменения слота предмета inventory->_moveTo(oldIndex, newIndex)
        - [ ] использовать память инвентаря для того чтобы понимать какой был предмет на oldIndex
        - [ ] отправляет setSlot(oldIndex, 0)
        - [ ] отправляет setSlot(newIndex, itemId)
    - [ ] функция взятия в курсор inventory->take(oldIndex) - делает inventory->_moveTo(oldIndex, cursorIndex)
    - [ ] функция чтобы положить из курсора в инвентарь в курсор inventory->put(newIndex) - делает inventory->_moveTo(cursorIndex, newIndex)
    - [ ] функция симуляции перемещения предмета в инвентаре inventory->move(oldIndex, newIndex) делает: (1) inventory->take(oldIndex) (2) sleep(1s) (3) inventory->put(newIndex)
    - [ ] функция симуляции перемещения inventory->move() должна иметь debounce на выполнение, чтобы пока не закончился прошлый не выполняется новый с warning предупреждением
- [ ] Добавить ItemIds для всех предметов формата enum: itemName -> itemId
- [ ] Переименовать bot.py в terrex.py
- [ ] Перевести все русские тексты и комментарии на английский язык

# Terraria.exe

- [ ] Декомпилировать код и посмотреть что за неизвестные пакеты отправляются сервером и клиентом 0x9A (154), 0x9B (155), 0x8E (142), 0x93 (147), 0xA0 (160),  без данных и с данными